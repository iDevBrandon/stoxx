"""Oxinion finance-parser — local, LLM-free PDF → structured JSON.

Docling does the heavy lifting (layout + tables + reading order) with local
ML models. This package normalizes its output into the block schema the
Oxinion Studio viewer consumes.
"""

__version__ = "0.1.0"
