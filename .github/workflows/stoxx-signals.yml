name: Stoxx — Fetch IOO Signals

on:
  schedule:
    # Runs at 10:45 AM New York time (EDT, UTC-4)
    - cron: "45 14 * * 1-5"
  workflow_dispatch: # allow manual trigger from GitHub UI

jobs:
  fetch-signals:
    name: Run stock-evaluator for IOO Global 100
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      FINANCE_SUPABASE_URL: ${{ secrets.FINANCE_SUPABASE_URL }}
      FINANCE_SUPABASE_SECRET_KEY: ${{ secrets.FINANCE_SUPABASE_SECRET_KEY }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: toolkit/stock-evaluator/requirements.txt

      - name: Install dependencies
        run: pip install -r toolkit/stock-evaluator/requirements.txt

      - name: Run stock evaluator (IOO)
        working-directory: toolkit/stock-evaluator
        run: python main.py

      - name: Notify on failure
        if: failure()
        run: echo "::error::stock-evaluator failed — check logs above"
